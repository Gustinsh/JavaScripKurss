
function faktorials() {
    const skaitlis = document.getElementById("skaitlis").value
    skaitlis = parseInt(skaitlis)
    const atbilde
    if (skaitlis === 0 || skaitlis === 1) {
        const x = + skaitlis + 
             else if (isNaN(skaitlis)) {
            document.getElementById("cikls").innerHTML = "Nepareizi kaut kas ievadīts, mēģini vēlreiz! 😉"
        }
        else {
            for (const i = skaitlis; i >= 1; i--) {
                atbilde = atbilde * i

                const reiznajums = skaitlis * skaitlis
            }
        }