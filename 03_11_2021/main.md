* saīsinājumi kodam if gadījumā:

b = typeof b !== 'undefined' ? b : 1;
        //b = typeof b === 'undefined' ? 1 : b;
        /*
        if (typeof b !== 'undefined')
         b = b;
        else
         b = 1;
        */
        /*
        if (typeof b !== 'undefined')
         ;
        else
         b = 1;
         */
        /*
        if (typeof b === 'undefined')
         b = 1;
        */